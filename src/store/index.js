import { createStore } from 'vuex'
import users from './modules/users/index'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    users
  }
})
